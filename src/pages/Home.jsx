import img from '../images/dashboard.png'

const Home = () => {
    return (
        <section className="cart">
            <img className='dashboard' src={img} />
        </section>
    )
}

export default Home